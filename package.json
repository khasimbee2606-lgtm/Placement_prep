{
  "name": "placement-preparation-tracker",
  "version": "1.0.0",
  "description": "Placement Preparation Tracker - Complete MERN Stack Application",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm --prefix backend run dev",
    "client": "npm --prefix frontend run dev",
    "start": "node backend/server.js",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "render-build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "install-all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "keywords": [
    "mern",
    "placement-tracker",
    "react",
    "node",
    "mongodb-atlas",
    "jwt",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
